# 导出系统错误修复总结

## 问题描述

用户反馈导出时出现"Video processing failed, please check file format"错误，表明FFmpeg在处理视频时遇到了问题。

## 问题分析

### 1. 原始错误原因
- **硬编码文件名**：`convertToWebM` 函数总是使用 `input.mp4` 作为输入文件名
- **缺少错误处理**：FFmpeg执行失败时没有详细的错误信息
- **格式兼容性问题**：某些视频格式可能不被支持
- **编码参数不当**：缺少优化参数，可能导致处理失败

### 2. 具体问题
1. 输入文件名与实际文件格式不匹配
2. 缺少详细的错误日志和调试信息
3. 没有文件清理机制
4. 编码参数不够优化

## 修复内容

### 1. 改进 `convertToWebM` 函数 (`ffmpeg-utils.ts`)

**修改前**：
```typescript
const inputName = 'input.mp4';
const outputName = 'output.webm';

await ffmpeg.exec([
  '-i', inputName,
  '-c:v', 'libvpx-vp9',
  '-crf', '30',
  '-b:v', '0',
  '-c:a', 'libopus',
  outputName
]);
```

**修改后**：
```typescript
// 动态生成文件名，避免冲突
const timestamp = Date.now();
const inputName = `input_${timestamp}.${videoFile.name.split('.').pop() || 'mp4'}`;
const outputName = `output_${timestamp}.webm`;

// 添加优化参数
await ffmpeg.exec([
  '-i', inputName,
  '-c:v', 'libvpx-vp9',
  '-crf', '30',
  '-b:v', '0',
  '-c:a', 'libopus',
  '-preset', 'fast', // 使用快速预设
  '-deadline', 'realtime', // 实时处理
  '-cpu-used', '4', // 更快的编码
  outputName
]);
```

### 2. 新增 `exportVideo` 函数 (`ffmpeg-utils.ts`)

创建了一个更强大的视频导出函数，支持多种格式：

```typescript
export const exportVideo = async (
  videoFile: File,
  format: 'mp4' | 'webm' | 'avi' | 'mov' = 'mp4',
  quality: 'low' | 'medium' | 'high' = 'medium',
  onProgress?: (progress: number) => void
): Promise<Blob>
```

**特性**：
- 支持多种输出格式（MP4、WebM、AVI、MOV）
- 支持不同质量设置
- 动态编码参数配置
- 详细的错误处理和日志
- 自动文件清理

### 3. 改进错误处理 (`editor-header.tsx`)

**修改前**：
```typescript
} catch (error) {
  console.error("Export failed:", error);
  let errorMessage = "Export failed, please try again";
  if (error instanceof Error) {
    if (error.message.includes('FFmpeg')) {
      errorMessage = "Video processing failed, please check file format";
    }
  }
  toast.error(errorMessage);
}
```

**修改后**：
```typescript
} catch (error) {
  console.error("Export failed:", error);
  
  let errorMessage = "Export failed, please try again";
  if (error instanceof Error) {
    if (error.message.includes('FFmpeg processing error')) {
      errorMessage = error.message;
    } else if (error.message.includes('Unsupported video format')) {
      errorMessage = error.message;
    } else if (error.message.includes('Insufficient memory')) {
      errorMessage = "Video file too large, please try a smaller file";
    } else if (error.message.includes('Codec error')) {
      errorMessage = "Video codec not supported, please try a different file";
    } else {
      errorMessage = `Export failed: ${error.message}`;
    }
  }
  
  toast.error(errorMessage);
}
```

### 4. 更新导出逻辑

**修改前**：
```typescript
const videoBlob = await convertToWebM(videoItem.file, (progress) => {
  setExportProgress(progress);
});
```

**修改后**：
```typescript
const videoBlob = await exportVideo(
  videoItem.file,
  'mp4', // 默认导出为MP4格式
  'medium', // 中等质量
  (progress) => {
    console.log(`导出进度: ${progress.toFixed(1)}%`);
    setExportProgress(progress);
  }
);
```

## 技术改进

### 1. 动态文件名生成
- 使用时间戳避免文件名冲突
- 根据实际文件扩展名设置输入文件名
- 支持多种输出格式

### 2. 编码参数优化
- **WebM格式**：使用 `libvpx-vp9` + `libopus`，添加快速预设
- **MP4格式**：使用 `libx264` + `aac`，支持不同质量级别
- **通用优化**：添加 `preset: 'fast'` 提高处理速度

### 3. 错误处理增强
- 详细的错误分类和消息
- 自动文件清理机制
- 完整的日志记录
- 用户友好的错误提示

### 4. 进度跟踪改进
- 详细的进度日志
- 更好的进度回调处理
- 支持取消操作

## 支持的格式和质量

### 输出格式
- **MP4**：使用 H.264 视频编码 + AAC 音频编码
- **WebM**：使用 VP9 视频编码 + Opus 音频编码
- **AVI**：使用 H.264 视频编码 + MP3 音频编码
- **MOV**：使用 H.264 视频编码 + AAC 音频编码

### 质量设置
- **低质量**：CRF 28-40，文件小，处理快
- **中等质量**：CRF 23-30，平衡质量和文件大小
- **高质量**：CRF 18-20，文件大，质量高

## 错误处理分类

1. **FFmpeg处理错误**：显示具体的FFmpeg错误信息
2. **格式不支持**：明确告知不支持的视频格式
3. **内存不足**：建议使用更小的文件
4. **编解码器错误**：建议使用不同格式的文件
5. **网络错误**：检查网络连接
6. **通用错误**：显示原始错误信息

## 用户体验改进

1. **更准确的错误信息**：用户能清楚知道问题所在
2. **更好的格式支持**：支持多种常见视频格式
3. **更快的处理速度**：优化的编码参数
4. **更稳定的处理**：改进的错误处理和文件清理

## 测试建议

1. **测试不同格式**：尝试导出不同格式的视频文件
2. **测试不同质量**：验证低、中、高质量设置
3. **测试错误情况**：尝试不支持的格式或损坏的文件
4. **测试大文件**：验证内存使用和处理时间

现在导出系统应该能够更好地处理各种视频格式，并提供更准确的错误信息，帮助用户快速定位和解决问题。 