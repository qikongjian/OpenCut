# OpenCut 项目技术文档

## 项目概述

OpenCut 是一个免费、开源的视频编辑器，支持 Web、桌面和移动端。该项目旨在提供一个隐私友好、功能完整的视频编辑解决方案，替代当前需要付费订阅的商业软件。

### 核心特性
- **隐私保护**: 视频处理完全在本地设备进行
- **免费使用**: 提供 CapCut 等商业软件的基础功能，无需付费
- **简单易用**: 专注于用户体验，降低使用门槛
- **多平台支持**: Web、桌面和移动端全覆盖
- **实时预览**: 基于时间轴的编辑体验
- **多轨道支持**: 支持复杂的视频编辑需求

## 技术架构

### 整体架构
OpenCut 采用现代化的 Monorepo 架构，使用 Turbo 进行构建优化：

```
OpenCut/
├── apps/
│   └── web/                 # Next.js Web 应用
├── packages/
│   ├── auth/               # 认证模块
│   └── db/                 # 数据库模块
├── docker-compose.yaml     # 容器编排配置
└── turbo.json             # Turbo 构建配置
```

### 技术栈

#### 前端技术栈
- **框架**: Next.js 15.3.4 (App Router)
- **语言**: TypeScript 5.x
- **UI 库**: React 18.2.0
- **样式**: Tailwind CSS 3.4.1
- **状态管理**: Zustand 5.0.2
- **表单处理**: React Hook Form 7.54.0
- **动画**: Framer Motion 11.13.1
- **拖拽**: @hello-pangea/dnd 18.0.1
- **视频处理**: FFmpeg WebAssembly

#### 后端技术栈
- **数据库**: PostgreSQL 17
- **缓存**: Redis 7
- **ORM**: Drizzle ORM 0.44.2
- **认证**: Better Auth 1.2.7
- **API**: Next.js API Routes

#### 开发工具
- **包管理器**: Bun 1.2.17
- **代码格式化**: Biome
- **构建工具**: Turbo
- **容器化**: Docker & Docker Compose

## 核心模块详解

### 1. Web 应用模块 (`apps/web/`)

#### 目录结构
```
apps/web/src/
├── app/                    # Next.js App Router 页面
│   ├── (auth)/            # 认证相关页面
│   ├── api/               # API 路由
│   ├── editor/            # 编辑器页面
│   └── contributors/      # 贡献者页面
├── components/            # React 组件
│   ├── editor/           # 编辑器组件
│   ├── landing/          # 落地页组件
│   └── ui/               # 通用 UI 组件
├── hooks/                # 自定义 React Hooks
├── lib/                  # 工具库
├── stores/               # Zustand 状态管理
└── types/                # TypeScript 类型定义
```

#### 核心功能模块

**编辑器组件 (`components/editor/`)**
- `timeline.tsx` - 时间轴组件
- `preview-panel.tsx` - 预览面板
- `media-panel.tsx` - 媒体面板
- `properties-panel.tsx` - 属性面板
- `speed-control.tsx` - 速度控制
- `project-name-editor.tsx` - 项目名称编辑器

**状态管理 (`stores/`)**
- `editor-store.ts` - 编辑器全局状态
- `timeline-store.ts` - 时间轴状态
- `media-store.ts` - 媒体文件状态
- `playback-store.ts` - 播放控制状态
- `project-store.ts` - 项目状态
- `panel-store.ts` - 面板状态

**自定义 Hooks (`hooks/`)**
- `use-playback-controls.ts` - 播放控制逻辑
- `use-drag-drop.ts` - 拖拽功能
- `use-toast.ts` - 消息提示

**工具库 (`lib/`)**
- `ffmpeg-utils.ts` - FFmpeg 工具函数
- `media-processing.ts` - 媒体处理
- `rate-limit.ts` - 速率限制
- `utils.ts` - 通用工具函数

### 2. 数据库模块 (`packages/db/`)

#### 数据库设计
使用 PostgreSQL 作为主数据库，通过 Drizzle ORM 进行管理。

**核心数据表**
- `users` - 用户信息表
- `sessions` - 会话管理表
- `accounts` - 第三方账户关联表
- `verifications` - 验证码表
- `waitlist` - 等待列表表

#### 数据库配置
- 使用 Drizzle Kit 进行数据库迁移
- 支持本地和生产的数据库推送
- 启用行级安全策略 (RLS)

### 3. 认证模块 (`packages/auth/`)

#### 认证方案
- 基于 Better Auth 库实现
- 支持多种认证提供商
- 客户端和服务端分离的认证逻辑

## 部署架构

### 容器化部署
项目使用 Docker Compose 进行容器编排：

**服务组件**
1. **PostgreSQL 数据库** (端口 5432)
   - 版本: 17
   - 数据持久化存储
   - 健康检查配置

2. **Redis 缓存** (端口 6379)
   - 版本: 7-alpine
   - 用于会话和缓存存储
   - 健康检查配置

3. **Serverless Redis HTTP** (端口 8079)
   - 为 Upstash Redis 提供 HTTP 接口
   - 支持远程 Redis 访问

4. **Web 应用** (端口 3001)
   - 基于 Next.js 构建
   - 生产环境配置
   - 依赖数据库和 Redis 服务

### 环境配置
项目支持多环境配置：
- 开发环境: 本地 Docker 服务
- 生产环境: 云服务部署

## 开发工作流

### 本地开发设置
1. **环境准备**
   - 安装 Bun、Docker、Node.js
   - 启动 PostgreSQL 和 Redis 服务

2. **项目初始化**
   ```bash
   bun install
   docker-compose up -d
   ```

3. **数据库迁移**
   ```bash
   bun run db:push:local
   ```

4. **启动开发服务器**
   ```bash
   bun run dev
   ```

### 代码质量保证
- **代码格式化**: Biome 配置
- **类型检查**: TypeScript 严格模式
- **代码规范**: ESLint + Biome 规则

## 核心功能实现

### 视频编辑功能
1. **时间轴编辑**
   - 基于 WebAssembly 的 FFmpeg 处理
   - 实时预览和渲染
   - 多轨道支持

2. **媒体处理**
   - 文件上传和格式转换
   - 视频编码和解码
   - 音频处理

3. **用户界面**
   - 响应式设计
   - 拖拽交互
   - 实时反馈

### 性能优化
- **Turbo 构建优化**: 增量构建和缓存
- **代码分割**: 按需加载组件
- **缓存策略**: Redis 缓存关键数据
- **CDN 优化**: 静态资源分发

## 安全考虑

### 数据安全
- 视频文件本地处理，不上传服务器
- 用户数据加密存储
- 行级安全策略 (RLS)

### 认证安全
- 基于 JWT 的会话管理
- 第三方 OAuth 集成
- 密码加密存储

### API 安全
- 速率限制保护
- CORS 配置
- 输入验证和清理

## 监控和分析

### 性能监控
- Vercel Analytics 集成
- 用户行为分析
- 错误追踪

### 健康检查
- 数据库连接监控
- Redis 服务状态
- API 端点健康检查

## 扩展性设计

### 模块化架构
- 清晰的模块边界
- 可插拔的组件设计
- 统一的接口规范

### 插件系统
- 支持自定义滤镜和效果
- 可扩展的媒体处理管道
- 第三方集成接口

## 未来规划

### 功能增强
- 更多视频格式支持
- 高级编辑功能
- 协作编辑功能

### 平台扩展
- 桌面应用开发
- 移动端应用
- PWA 支持

### 性能优化
- WebAssembly 优化
- 渲染性能提升
- 内存使用优化

## 贡献指南

### 开发规范
- 遵循 TypeScript 最佳实践
- 使用 Biome 进行代码格式化
- 编写单元测试和集成测试

### 提交规范
- 使用语义化提交信息
- 代码审查流程
- 自动化测试和部署

---

*本文档基于 OpenCut 项目当前版本编写，随着项目发展会持续更新。* 