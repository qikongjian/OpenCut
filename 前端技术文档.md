# OpenCut 前端技术文档

## 目录
1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [目录结构](#目录结构)
4. [核心功能模块](#核心功能模块)
5. [状态管理系统](#状态管理系统)
6. [组件系统](#组件系统)
7. [视频处理技术](#视频处理技术)
8. [UI/UX 设计系统](#uiux-设计系统)
9. [性能优化](#性能优化)
10. [开发规范](#开发规范)
11. [部署配置](#部署配置)

## 项目概述

OpenCut 前端是一个基于 Next.js 15 的现代化视频编辑器 Web 应用，采用 TypeScript 开发，提供专业级的视频编辑功能。

### 核心特性
- **实时视频编辑**: 基于时间轴的多轨道编辑
- **本地处理**: 使用 WebAssembly FFmpeg 进行本地视频处理
- **响应式设计**: 支持多种屏幕尺寸和分辨率
- **高性能**: 优化的渲染和状态管理
- **模块化架构**: 可扩展的组件和功能模块

## 技术架构

### 技术栈
```
前端框架: Next.js 15.3.4 (App Router)
开发语言: TypeScript 5.x
UI 框架: React 18.2.0
样式系统: Tailwind CSS 3.4.1
状态管理: Zustand 5.0.2
视频处理: FFmpeg WebAssembly
构建工具: Turbo + Bun
代码规范: Biome
```

### 架构模式
- **Monorepo 架构**: 使用 Turbo 进行构建优化
- **组件驱动**: 基于 React 组件化开发
- **状态集中**: Zustand 统一状态管理
- **类型安全**: TypeScript 严格类型检查

## 目录结构

```
apps/web/src/
├── app/                    # Next.js App Router 页面
│   ├── (auth)/            # 认证相关页面
│   │   ├── login/         # 登录页面
│   │   └── signup/        # 注册页面
│   ├── api/               # API 路由
│   │   ├── auth/          # 认证 API
│   │   ├── health/        # 健康检查
│   │   └── waitlist/      # 等待列表
│   ├── editor/            # 编辑器主页面
│   ├── contributors/      # 贡献者页面
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 首页
├── components/            # React 组件
│   ├── editor/           # 编辑器专用组件
│   │   ├── timeline.tsx  # 时间轴组件
│   │   ├── preview-panel.tsx # 预览面板
│   │   ├── media-panel.tsx   # 媒体面板
│   │   ├── properties-panel.tsx # 属性面板
│   │   ├── speed-control.tsx # 速度控制
│   │   └── project-name-editor.tsx # 项目名称编辑器
│   ├── landing/          # 落地页组件
│   ├── ui/               # 通用 UI 组件
│   └── editor-header.tsx # 编辑器头部
├── hooks/                # 自定义 React Hooks
│   ├── use-playback-controls.ts # 播放控制
│   ├── use-drag-drop.ts  # 拖拽功能
│   └── use-toast.ts      # 消息提示
├── lib/                  # 工具库
│   ├── ffmpeg-utils.ts   # FFmpeg 工具函数
│   ├── media-processing.ts # 媒体处理
│   ├── utils.ts          # 通用工具
│   └── rate-limit.ts     # 速率限制
├── stores/               # Zustand 状态管理
│   ├── editor-store.ts   # 编辑器状态
│   ├── timeline-store.ts # 时间轴状态
│   ├── media-store.ts    # 媒体状态
│   ├── playback-store.ts # 播放状态
│   ├── project-store.ts  # 项目状态
│   └── panel-store.ts    # 面板状态
└── types/                # TypeScript 类型定义
    ├── project.ts        # 项目类型
    └── playback.ts       # 播放类型
```

## 核心功能模块

### 1. 编辑器核心 (`app/editor/`)

**主要文件**: `page.tsx`

编辑器采用可调整大小的面板布局，包含以下主要区域：

```typescript
// 面板布局结构
<ResizablePanelGroup direction="vertical">
  <ResizablePanel> {/* 主内容区域 */}
    <ResizablePanelGroup direction="horizontal">
      <ResizablePanel> {/* 工具面板 */}
        <MediaPanel />
      </ResizablePanel>
      <ResizablePanel> {/* 预览区域 */}
        <PreviewPanel />
      </ResizablePanel>
    </ResizablePanelGroup>
  </ResizablePanel>
  <ResizablePanel> {/* 时间轴 */}
    <Timeline />
  </ResizablePanel>
</ResizablePanelGroup>
```

**核心特性**:
- 可调整大小的面板布局
- 实时预览和编辑
- 多轨道时间轴
- 拖拽式媒体导入

### 2. 时间轴系统 (`components/editor/timeline.tsx`)

时间轴是编辑器的核心组件，提供以下功能：

**数据结构**:
```typescript
interface TimelineTrack {
  id: string;
  name: string;
  type: "video" | "audio" | "effects";
  clips: TimelineClip[];
  muted?: boolean;
}

interface TimelineClip {
  id: string;
  mediaId: string;
  name: string;
  duration: number;
  startTime: number;
  trimStart: number;
  trimEnd: number;
}
```

**核心功能**:
- 多轨道支持 (视频、音频、特效)
- 拖拽式剪辑操作
- 实时缩放和滚动
- 多选和批量操作
- 撤销/重做功能
- 键盘快捷键支持

**交互特性**:
- 拖拽添加媒体到轨道
- 调整剪辑长度 (trim)
- 移动剪辑位置
- 分割和合并剪辑
- 轨道静音控制

### 3. 预览面板 (`components/editor/preview-panel.tsx`)

预览面板提供实时视频预览功能：

**功能特性**:
- 实时视频播放
- 多种画布比例预设
- 播放控制 (播放/暂停/静音)
- 调试信息显示
- 多轨道合成预览

**画布预设**:
```typescript
const canvasPresets = [
  { name: "16:9 HD", width: 1920, height: 1080 },
  { name: "16:9 4K", width: 3840, height: 2160 },
  { name: "9:16 Mobile", width: 1080, height: 1920 },
  { name: "1:1 Square", width: 1080, height: 1080 },
  { name: "4:3 Standard", width: 1440, height: 1080 },
];
```

### 4. 媒体面板 (`components/editor/media-panel.tsx`)

媒体面板管理项目中的所有媒体文件：

**功能特性**:
- 媒体文件上传
- 文件类型支持 (视频、音频、图片)
- 缩略图生成
- 文件信息显示
- 拖拽到时间轴

## 状态管理系统

### Zustand 状态架构

项目使用 Zustand 进行状态管理，分为多个专门的 store：

#### 1. 编辑器状态 (`stores/editor-store.ts`)
```typescript
interface EditorState {
  isInitializing: boolean;
  isPanelsReady: boolean;
  setInitializing: (loading: boolean) => void;
  setPanelsReady: (ready: boolean) => void;
  initializeApp: () => Promise<void>;
}
```

#### 2. 时间轴状态 (`stores/timeline-store.ts`)
```typescript
interface TimelineStore {
  tracks: TimelineTrack[];
  history: TimelineTrack[][];
  redoStack: TimelineTrack[][];
  selectedClips: { trackId: string; clipId: string }[];
  
  // 轨道操作
  addTrack: (type: "video" | "audio" | "effects") => string;
  removeTrack: (trackId: string) => void;
  
  // 剪辑操作
  addClipToTrack: (trackId: string, clip: Omit<TimelineClip, "id">) => void;
  removeClipFromTrack: (trackId: string, clipId: string) => void;
  updateClipTrim: (trackId: string, clipId: string, trimStart: number, trimEnd: number) => void;
  
  // 选择操作
  selectClip: (trackId: string, clipId: string, multi?: boolean) => void;
  clearSelectedClips: () => void;
  
  // 历史操作
  undo: () => void;
  redo: () => void;
  pushHistory: () => void;
}
```

#### 3. 媒体状态 (`stores/media-store.ts`)
```typescript
interface MediaStore {
  mediaItems: MediaItem[];
  addMediaItem: (item: MediaItem) => void;
  removeMediaItem: (id: string) => void;
  updateMediaItem: (id: string, updates: Partial<MediaItem>) => void;
}
```

#### 4. 播放状态 (`stores/playback-store.ts`)
```typescript
interface PlaybackStore {
  currentTime: number;
  duration: number;
  isPlaying: boolean;
  speed: number;
  volume: number;
  muted: boolean;
  
  seek: (time: number) => void;
  toggle: () => void;
  setSpeed: (speed: number) => void;
  setVolume: (volume: number) => void;
  toggleMute: () => void;
}
```

#### 5. 面板状态 (`stores/panel-store.ts`)
```typescript
interface PanelStore {
  toolsPanel: number;
  previewPanel: number;
  propertiesPanel: number;
  mainContent: number;
  timeline: number;
  
  setToolsPanel: (size: number) => void;
  setPreviewPanel: (size: number) => void;
  setPropertiesPanel: (size: number) => void;
  setMainContent: (size: number) => void;
  setTimeline: (size: number) => void;
}
```

### 状态管理最佳实践

1. **状态分离**: 按功能模块分离状态
2. **不可变更新**: 使用不可变的方式更新状态
3. **历史管理**: 实现撤销/重做功能
4. **性能优化**: 避免不必要的重新渲染

## 组件系统

### UI 组件库

项目使用基于 Radix UI 的自定义组件库：

#### 基础组件
- `Button`: 按钮组件，支持多种变体
- `Input`: 输入框组件
- `Select`: 选择器组件
- `Tooltip`: 工具提示组件
- `ScrollArea`: 滚动区域组件

#### 编辑器专用组件
- `VideoPlayer`: 视频播放器组件
- `ResizablePanel`: 可调整大小的面板
- `TimelineTrack`: 时间轴轨道组件
- `TimelineClip`: 时间轴剪辑组件

### 组件设计原则

1. **组合优于继承**: 使用组合模式构建复杂组件
2. **单一职责**: 每个组件只负责一个功能
3. **可复用性**: 组件设计为可复用
4. **类型安全**: 完整的 TypeScript 类型定义

## 视频处理技术

### FFmpeg WebAssembly 集成

项目使用 FFmpeg WebAssembly 进行本地视频处理：

#### 核心功能 (`lib/ffmpeg-utils.ts`)

**1. 初始化**
```typescript
export const initFFmpeg = async (): Promise<FFmpeg> => {
  if (ffmpeg) return ffmpeg;
  
  ffmpeg = new FFmpeg();
  await ffmpeg.load({
    coreURL: await toBlobURL(`${baseURL}/ffmpeg-core.js`, 'text/javascript'),
    wasmURL: await toBlobURL(`${baseURL}/ffmpeg-core.wasm`, 'application/wasm'),
  });
  
  return ffmpeg;
};
```

**2. 视频信息提取**
```typescript
export const getVideoInfo = async (videoFile: File): Promise<{
  duration: number;
  width: number;
  height: number;
  fps: number;
}> => {
  // 使用 FFmpeg 提取视频元数据
};
```

**3. 视频裁剪**
```typescript
export const trimVideo = async (
  videoFile: File,
  startTime: number,
  endTime: number,
  onProgress?: (progress: number) => void
): Promise<Blob> => {
  // 使用 FFmpeg 裁剪视频
};
```

**4. 缩略图生成**
```typescript
export const generateThumbnail = async (
  videoFile: File,
  timeInSeconds: number = 1
): Promise<string> => {
  // 生成视频缩略图
};
```

**5. 格式转换**
```typescript
export const convertToWebM = async (
  videoFile: File,
  onProgress?: (progress: number) => void
): Promise<Blob> => {
  // 转换为 WebM 格式
};
```

### 媒体处理流程

1. **文件上传**: 支持拖拽和点击上传
2. **格式检测**: 自动检测文件类型
3. **信息提取**: 提取视频元数据
4. **缩略图生成**: 生成预览缩略图
5. **本地存储**: 使用 IndexedDB 存储媒体文件

## UI/UX 设计系统

### 设计原则

1. **简洁性**: 界面简洁，功能清晰
2. **一致性**: 统一的设计语言
3. **可访问性**: 支持键盘导航和屏幕阅读器
4. **响应式**: 适配不同屏幕尺寸

### 颜色系统

基于 CSS 变量的主题系统：

```css
:root {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --card: 0 0% 100%;
  --card-foreground: 222.2 84% 4.9%;
  --popover: 0 0% 100%;
  --popover-foreground: 222.2 84% 4.9%;
  --primary: 222.2 47.4% 11.2%;
  --primary-foreground: 210 40% 98%;
  --secondary: 210 40% 96%;
  --secondary-foreground: 222.2 84% 4.9%;
  --muted: 210 40% 96%;
  --muted-foreground: 215.4 16.3% 46.9%;
  --accent: 210 40% 96%;
  --accent-foreground: 222.2 84% 4.9%;
  --destructive: 0 84.2% 60.2%;
  --destructive-foreground: 210 40% 98%;
  --border: 214.3 31.8% 91.4%;
  --input: 214.3 31.8% 91.4%;
  --ring: 222.2 84% 4.9%;
  --radius: 0.5rem;
}
```

### 字体系统

使用 Inter 字体作为主要字体：

```typescript
const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});
```

### 响应式设计

使用 Tailwind CSS 的响应式断点：

- `sm`: 640px 及以上
- `md`: 768px 及以上
- `lg`: 1024px 及以上
- `xl`: 1280px 及以上
- `2xl`: 1536px 及以上

## 性能优化

### 1. 代码分割

使用 Next.js 的动态导入进行代码分割：

```typescript
const VideoPlayer = dynamic(() => import('./VideoPlayer'), {
  loading: () => <div>Loading...</div>,
  ssr: false
});
```

### 2. 虚拟化

对于大量数据的列表，使用虚拟化技术：

```typescript
// 时间轴轨道虚拟化
const VirtualizedTimeline = () => {
  // 只渲染可见的轨道
};
```

### 3. 内存管理

- 及时释放 FFmpeg 资源
- 使用 WeakMap 缓存对象
- 避免内存泄漏

### 4. 渲染优化

- 使用 React.memo 避免不必要的重新渲染
- 使用 useMemo 和 useCallback 优化计算
- 使用 React.lazy 延迟加载组件

### 5. 缓存策略

- 使用 IndexedDB 缓存媒体文件
- 使用 localStorage 缓存用户设置
- 使用 Service Worker 缓存静态资源

## 开发规范

### 代码风格

使用 Biome 进行代码格式化和检查：

```json
{
  "formatter": {
    "enabled": true,
    "indentStyle": "tab"
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  }
}
```

### 命名规范

1. **组件**: PascalCase (如 `Timeline`, `VideoPlayer`)
2. **函数**: camelCase (如 `handleClick`, `generateThumbnail`)
3. **常量**: UPPER_SNAKE_CASE (如 `MAX_FILE_SIZE`)
4. **类型**: PascalCase (如 `TimelineClip`, `MediaItem`)

### 文件组织

1. **组件文件**: 一个组件一个文件
2. **工具函数**: 按功能分组
3. **类型定义**: 集中管理
4. **测试文件**: 与源文件同目录

### Git 提交规范

使用语义化提交信息：

```
feat: 添加视频裁剪功能
fix: 修复时间轴拖拽问题
docs: 更新 API 文档
style: 调整按钮样式
refactor: 重构状态管理
test: 添加单元测试
chore: 更新依赖版本
```

## 部署配置

### 构建配置

```json
{
  "scripts": {
    "dev": "next dev --turbopack -p 3001",
    "build": "next build",
    "start": "next start -p 3001"
  }
}
```

### 环境变量

```bash
# 开发环境
NODE_ENV=development
DATABASE_URL=postgresql://opencut:opencutthegoat@localhost:5432/opencut
BETTER_AUTH_URL=http://localhost:3001

# 生产环境
NODE_ENV=production
DATABASE_URL=postgresql://opencut:opencutthegoat@db:5432/opencut
BETTER_AUTH_URL=http://localhost:3001
```

### Docker 配置

```dockerfile
FROM node:18-alpine AS base

# 安装依赖
FROM base AS deps
RUN apk add --no-cache libc6-compat
WORKDIR /app
COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

# 构建应用
FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN bun run build

# 生产镜像
FROM base AS runner
WORKDIR /app
ENV NODE_ENV production

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs

EXPOSE 3001

ENV PORT 3001
ENV HOSTNAME "0.0.0.0"

CMD ["node", "server.js"]
```

## 测试策略

### 单元测试

使用 Jest 和 React Testing Library：

```typescript
import { render, screen } from '@testing-library/react';
import { Timeline } from './Timeline';

describe('Timeline', () => {
  it('should render timeline tracks', () => {
    render(<Timeline />);
    expect(screen.getByTestId('timeline')).toBeInTheDocument();
  });
});
```

### 集成测试

测试组件间的交互：

```typescript
describe('Editor Integration', () => {
  it('should add media to timeline', async () => {
    // 测试拖拽媒体到时间轴
  });
});
```

### E2E 测试

使用 Playwright 进行端到端测试：

```typescript
import { test, expect } from '@playwright/test';

test('video editing workflow', async ({ page }) => {
  await page.goto('/editor');
  await page.dragAndDrop('[data-testid="media-item"]', '[data-testid="timeline"]');
  await expect(page.locator('[data-testid="timeline-clip"]')).toBeVisible();
});
```

## 监控和分析

### 性能监控

使用 Vercel Analytics 监控应用性能：

```typescript
import { Analytics } from "@vercel/analytics/react";

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
```

### 错误追踪

集成错误监控服务：

```typescript
// 全局错误处理
window.addEventListener('error', (event) => {
  console.error('Global error:', event.error);
  // 发送到错误监控服务
});
```

### 用户行为分析

使用 DataBuddy 进行用户行为分析：

```html
<script
  src="https://app.databuddy.cc/databuddy.js"
  data-client-id="UP-Wcoy5arxFeK7oyjMMZ"
  data-track-attributes={true}
  data-track-errors={true}
  data-track-outgoing-links={true}
  data-track-web-vitals={true}
/>
```

## 未来规划

### 功能增强

1. **高级编辑功能**
   - 视频特效和滤镜
   - 音频处理和混音
   - 字幕和文字叠加

2. **协作功能**
   - 实时协作编辑
   - 项目分享和导出
   - 版本控制

3. **性能优化**
   - WebGPU 加速渲染
   - 更高效的视频编码
   - 智能缓存策略

### 技术升级

1. **框架升级**
   - React 19 新特性
   - Next.js 16 优化
   - TypeScript 5.5 新功能

2. **新技术支持**
   - WebCodecs API
   - WebAssembly SIMD
   - SharedArrayBuffer

---

*本文档基于 OpenCut 前端项目当前版本编写，随着项目发展会持续更新。* 